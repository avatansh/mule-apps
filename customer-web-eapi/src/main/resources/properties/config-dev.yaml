api:
  id: "18680329"

tls.keystore:
  type:        "pkcs12"
  path:        "certs/customer-web-eapi-dev.p12"
  alias:       "server"

request:
  config:
    papi:
      account:
        host: "customer-accounts-papi-dev-vpsht8.5sc6y6-3.usa-e2.cloudhub.io"
        port: ""
        response:
          timeout: "70000"
        idle:
          timeout: "60000"
        retry:
          frequency: "2000"
          attempts: "2"